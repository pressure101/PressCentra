# PressCentra
Monorepo for various projects

First Project:
- React-based frontend written in Typescript
- Typescript backend to handle pulling data from the database and serving the FE
- An ETL pipeline written in Python to scrape sites, normalize the data, and load to the DB
- PostgresDB to house loaded data