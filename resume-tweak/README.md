# Resume Tweak

This project is to meant to tweak your resume to fit a corresponding job requsitions to maximize the ability to score an interview

### Install Dependencies
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
