[project]
name = "resume-tweak"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pressure101" }
]
requires-python = ">=3.14"
dependencies = [
    "ollama~=0.6.2",
    "streamlit>=1.61.1",
]

[project.scripts]
resume-tweak = "resume_tweak:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
